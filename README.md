# DSL Generator

Генератор C-кода из блок-схемы (DSL) в формате XML.

## Сборка и запуск

Установите библиотеку pugixml (в Ubuntu/Debian: `sudo apt install libpugixml-dev`). 

Затем выполните:

```bash
-> make
-> ./appendix/dsl_gen
